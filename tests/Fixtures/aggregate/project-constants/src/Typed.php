<?php declare(strict_types=1);

namespace Coverage;

/** The typed half, so the percentage is not 0 and the message carries a real split. Extended below. */
class Typed
{
    public const string NAME = 'typed';
}
